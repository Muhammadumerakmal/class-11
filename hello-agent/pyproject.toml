[project]
name = "hello-agent"
version = "0.1.0"
description = "OpenAI Agents SDK fundamentals, one script per part of the guide"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.22.2",
    "python-dotenv>=1.2.3",
]
